# Docker Compose for LOCAL/DEV environments
# Use this for local development and testing
# ENV can be 'local' or 'dev' (defaults to 'local')

name: komodo-auth-user-api-dev

services:
  auth-user-api:
    build:
      context: ..
      dockerfile: build/Dockerfile
      args:
        - ENV=${ENV:-local}
    container_name: komodo-auth-user-api-${ENV:-local}
    environment:
      - ENV=${ENV:-local}
      - VERSION=${VERSION:-dev}
      - PORT=${PORT:-7001}
      - USE_AWS_SECRETS=false
      - USE_ELASTICACHE=false
      - AWS_REGION=${AWS_REGION:-us-east-1}
      - AWS_SECRET_PREFIX=${AWS_SECRET_PREFIX:-komodo-auth-user-api/${ENV}/}
      - EVAL_RULES_PATH=/app/config/validation_rules.yml
      - LOG_LEVEL=${LOG_LEVEL:-debug}
      - USE_MOCKS=${USE_MOCKS:-true}
    env_file:
      - path: ../env/.env.${ENV:-local}
        required: false
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:7001/health"]
      interval: 10s
      timeout: 2s
      retries: 5
    image: komodo-auth-user-api:${ENV:-local}
    ports:
      - "${PORT:-7001}:7001"
    volumes:
      - ../tests/mocks:/tests/mocks
