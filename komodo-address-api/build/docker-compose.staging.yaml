version: "3.9"
services:
  address-api:
    build:
      context: ..
      dockerfile: build/Dockerfile
    container_name: komodo-address-api-qa
    env_file:
      - ../config/.env.qa
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://TODO"]
      interval: 10s
      timeout: 2s
      retries: 5
    image: komodo-address-api:qa
    ports:
      - "7010:7010"
