version: "3.9"
services:
  auth-api:
    build:
      context: ..
      dockerfile: build/Dockerfile
    container_name: komodo-auth-api-prod
    environment:
      - EVAL_RULES_PATH=/app/config/validation_rules.yml
    env_file:
      - ../env/.env.prod
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:7001/health"]
      interval: 10s
      timeout: 2s
      retries: 5
    image: komodo-auth-api:prod
    ports:
      - "7001:7001"
